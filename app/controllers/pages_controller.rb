class PagesController < ApplicationController
  def index
    # @model = index_params['model'].constantize
    # @count = index_params['count'].to_i
    # @subjects = @model.first(@count)
    # @aircrafts = Aircraft.all
  end
end
